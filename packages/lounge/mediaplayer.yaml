media_player:
  - platform: kodi
    host: !secret kodihost
    port: 8080
    name: Kodi
    username: !secret kodiuser
    password: !secret kodipass
    turn_off_action: reboot
  - platform: cast
  - platform: spotify
    client_id: !secret spotifyid
    client_secret: !secret spotifysecret
  # - platform: spotify
  #   name: spotify player
  #   client_id: spotify2id
  #   client_secret: spotify2secret
